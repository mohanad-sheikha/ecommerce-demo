import React from 'react'
import ProductsPageSidebar from '../components/ProductsPageSidebar'

export default { title: 'Components/Products Page Sidebar', component: ProductsPageSidebar }

const Template = (args) => <ProductsPageSidebar { ...args } />

export const Default = Template.bind({})